export default function CreateProfile() {
    async function handleSubmit(formData) {

    }


    return(
        <main>
            <form action={handleSubmit}>
                <input type="text" />
            </form>
        </main>
    )
}